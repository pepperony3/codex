services:
  - type: web
    name: taskboard-api
    runtime: docker
    plan: free
    dockerfilePath: Dockerfile.taskboard
    envVars:
      - key: SPRING_DATASOURCE_URL
        value: jdbc:h2:mem:taskboard
      - key: APP_JWT_SECRET
        generateValue: true
