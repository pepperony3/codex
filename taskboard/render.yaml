services:
  - type: web
    name: taskboard-api
    runtime: docker
    plan: free
    dockerfilePath: taskboard/docker/Dockerfile.backend
    dockerContext: taskboard
    envVars:
      - key: SPRING_DATASOURCE_URL
        value: jdbc:h2:mem:taskboard
      - key: APP_JWT_SECRET
        generateValue: true
